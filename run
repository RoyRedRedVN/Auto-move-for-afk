-- LocalScript trong StarterPlayerScripts
local Players = game:GetService("Players")

local player = Players.LocalPlayer
local char = player.Character or player.CharacterAdded:Wait()
local humanoid = char:WaitForChild("Humanoid")
local root = char:WaitForChild("HumanoidRootPart")

local WALK_RADIUS = 50

-- Random 1 điểm quanh player
local function getRandomPoint()
	local base = root.Position
	local offset = Vector3.new(
		math.random(-WALK_RADIUS, WALK_RADIUS),
		0,
		math.random(-WALK_RADIUS, WALK_RADIUS)
	)
	return base + offset
end

-- Nếu humanoid ngồi thì bật nhảy
humanoid:GetPropertyChangedSignal("Sit"):Connect(function()
	if humanoid.Sit then
		print("⚠️ Player bị sit → auto jump!")
		humanoid.Jump = true
	end
end)

-- Main loop: đi tới điểm, nghỉ 2s, rồi đi tiếp
task.spawn(function()
	while true do
		local point = getRandomPoint()
		print("👉 MoveTo:", point)
		humanoid:MoveTo(point)
		humanoid.MoveToFinished:Wait()
		task.wait(2) -- nghỉ 2 giây
	end
end)